name: pygate-alternate-docs

services:
  docs:
    build: .
    container_name: pygate-alternate-docs
    restart: unless-stopped
    ports:
      # nginx listens on 80 and 8080 inside the container; expose 8080
      # on the host so local dev matches the Coolify Port=8080 recipe.
      - "3004:8080"
    environment:
      - DOCS_URL=https://alt-docs.payment.et
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:8080/"]
      interval: 30s
      timeout: 5s
      retries: 3
